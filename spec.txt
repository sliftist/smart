NEED: ferrite cores

TODO: Web interface to manually control switches

- Properly bundle the required RTL libraries for Linux. Apparently their release is missing some of the shared libraries.
    - sudo apt install librtlsdr0

1) Floor plan with visualized metrics (not over time, but we can seek times to see them change)


TODO: State tracking
    1) Create a model that takes as inputs any changes to the state such as turning on the heater, turning on the cooler.
        - The model will also have constant parameters. 
        - I imagine the model should just be energy-based, where we just have the pool of energy, or rather, I guess, two pools. And then there's a lag time to go between them. And that base, the time that it takes for the energy to go between them depends on how much energy differential there is. And then there's also how much energy is being lost from the system, and then the heater adds energy to the system. 
    2) For any chunk of time, probably around an hour, we should find which model constant parameters will create the most accurate tracking of the actual data.
    2.1) We should then for historical data plot these model constants over time. Just to verify that they're not completely random or redundant.
    2.2) We should also see if there's any other external factors which might control these model constants. 
        a) Outside temperature
        b) Sunlight level outside
        c) Additional events (showering, when we open our bedroom door, etc)
    3) Then we just take the last hour, use that to find the parameters, And then use that to extrapolate to see what the future temperature will be. Then we look at our different actions. We can probably look at every different action. and use those to see what that will cause the future temperature to be. And then we can pick the one which is closest to our target parameters (probably just average temperature).
    NOTE: This means we don't actually ever need something that explicitly predicts the future temperatures. I think this is fine, as long as we make sure our model is accurate, and not just a polynomial/sinusoidal With enough parameters that will fit basically any periodic function, But when extrapolated, will grow rapidly. If the model is actually based on reality, I think it will extrapolate quite well. 
    4) We can then take this extrapolation model and determine how accurate it is based on the historical data.
        a) Determine if it has any bias in either direction. 
        b) Determine how much the error increases when extrapolating future data vs the error on fitting the known data.
    5) I think we should manually adjust the heat for. Really weird patterns to create difficult data to predict. That way we can really determine if it's actually accounting for the parameters. or just kind of roughly staying the same temperature, which works because we want the temperature to stay roughly the same. So that's what our thermostat presently does. 

    Provide a full view of the state parameters, PLUS current temp, and predicted time to reach target temp and predicted overshoot, predicted cycle time, etc, etc


TODO: Variable rate humidifier
    - THEN, if it is 1 tick above the target, reduce the rate by 10%, then at 2 ticks reduce by 20%, etc.
            - OH! And at 2 ticks above, reduce by more slightly, as time goes on, as we don't just want to stop it, we want to reduce it.
        - I believe it might oscilate, BUT, will eventually reach the perfect rate
        - We should chart this and look at some of the math. I am sure there is a perfect adjustment.
        - ALSO, cheating, but looking at more sensors, can help as well. But then again... a tick based approach will work for steady state conditions.
    - OH! So what we actually want to do, is essentially calculate the velocity.
        - It's a little bit weird because the values are not very granular, so you know we might get one tick after five minutes. 
        - But once we do that, then we can see, you know, if if if one tick happened in five minutes, then the velocity is one tick per five minutes. And if the current percentage is 50%... Then we essentially create a conversion between percentage and raw velocity, And use this to compute the Velocity: We should change it to to create a parity between the raw velocity and the drain rate. 
    - There is also the lag, as in we turn it off, but the humidity keeps going up because the humidity sensor has a lag.
        - This might have a slight negative acceleration as well, but I think we can probably just represent it as a single number. 
        - Calculating the leg is a lot harder, but I think over time, when we turn it off, we can kind of see how much leg there is. I guess basically, if the value goes up after a certain amount of time, we average that time, and that's the lag. If it never goes up, that helps reduce the lag average. If it sometimes takes three minutes, then, you know, that's going to increase the lag average. If sometimes it goes up by two, then the average is even higher. 
            - I think we might also have to take into account when it starts going back down?
    - I think what we basically have is we have the velocity of the humidifier, and we also have the negative velocity of the room, which actually might even be positive velocity. We can't determine them independently, directly, but we can determine the sum of them. And then from this, eventually, we can determine to some degree the independent values, at least the independent value of the velocity of the humidifier. And then we can determine the sum, and that'll give us the velocity of the humidity of the room. And then we can start to predict how the velocity of the humidity of the room will change. For example, if the shower turns on, if the outside temperature forecast changes, etc., or just the time of the day, we can predict what the humidity of the room is, the velocity of the humidity of the room. And as our measurements are about five minutes delayed, this can allow us to react faster than that five minutes.
        - Although even that, even when I shower, with a hot shower, it still doesn't cause net positive humidity by the humidifier. I guess if the humidity in my room was lower, it might... But if it doesn't even cause a positive humidity then we don't even really need to predict it...

TODO: Turn computer on with smart switch
    - We can have the computer turn on when the power supply is given power (in BIOS)
    - I'm not sure if this can work with sleep? Even if not, this might still be useful.
        - Just using the smart plug for power monitoring would be nice anyways